// D1 Schema reference (actual SQL in migrations)
// Tables: users, sessions, transfers, transfer_files, download_events, security_events, abuse_reports, system_settings, rate_limits, api_keys
export const TABLES = `
-- FileDrop D1 Schema
`
